struct Instruction:
    name: str

    def __str__(self) -> str:
        mode = f"{self.name}"
        print(mode.split('.'))
        return mode

def main() -> i32:
    i = Instruction("LDA")
    print(i)
    return 0
