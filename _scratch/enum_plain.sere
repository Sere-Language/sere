enum Color:
    Red
    Green

    def is_red(self) -> bool:
        match self:
            case Color.Red:
                return True
            case Color.Green:
                return False

def main() -> i32:
    c: Color = Color.Red
    print(c.is_red())
    return 0
