class Vec:
    @private x: i32
    @private y: i32

    def __init__(self, x: i32, y: i32) -> None:
        self.x = x
        self.y = y

    def __len__(self) -> i64:
        return 2


def main(argv: list[str]) -> None:
    a = Vec(1, 2)
    print("len:")
    print(len(a))
