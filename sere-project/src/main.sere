

import wsgi

class App:
    def handle(self, environ: wsgi.Environ) -> wsgi.Response:
        return wsgi.Response(200, "text/plain", "ok")

def main() -> i32:
    app = App()
    env = wsgi.Environ("GET", "/", "")
    resp = app.handle(env)
    print(resp.status)
    return 0
