macro twice(x):
    quote:
        ($x) + ($x)

macro vec:
    match:
        ($($x:expr),*) => quote:
            [$($x),*]

def main() -> i32:
    n: i32 = 3
    total: i32 = twice!(n)
    xs: list[i32] = vec!(1, 2, 3)
    dbg!(total)
    return total + xs[0]

