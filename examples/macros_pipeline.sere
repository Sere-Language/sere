macro pipeline:
    syntax: pipeline

def add2(n: i32) -> i32:
    return n + 2

def wrap(left: i32, right: i32) -> i32:
    return left + right

def main() -> i32:
    n: i32 = pipeline:
        1
        |> add2
        |> wrap(4)
    return n
